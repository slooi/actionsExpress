

on:
  push:
    branches: [push]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: echo
        run: echo hi
      - name: echo2
        run: echo hi
